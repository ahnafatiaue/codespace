#include <stdio.h>

int main() {
   int a,b,i,count=0,c,d,p=1,q=1,sum,e;
   scanf("%d %d",&a,&b);
   for(i=a;i<=b;i++)
   {
    c = i;
    d = i;
    x=i;
    y =i;



    while(i!=0)
    {
        e=i%10;
        sum+=e;
        i/=10;


        count++; // count the digits
    }
    while()
    {

        int  remainder = x % 10;
       int  reversed_num = reversed_num * 10 + remainder;
        x /= 10;
    }
    }

    if((c%10) == 1 && (d/q) == 1)
    {
        if((sum*sum)%8=0)
        {
            if(reversed_num == y)
            {
                printf("%d",y);
            }

        }
    }
   }
   }


    return 0;
}
