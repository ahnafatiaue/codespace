#include <stdio.h>

int main() {
   int a,b,i,count=0;
   scanf("%d%d",&a,&b);
   for(i=a;i<=b;i++)
   {
    c = i;
    d = i;
    
    while(i!=0)
    {
        i%10;
        count++; // count the digits
    }
   }



    return 0;
}
